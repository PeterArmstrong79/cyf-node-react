## Demo of a node + react app

To start the server:

- `cd server`
- `npm install` (the first time)
- `node server.js`

To start the client:

- `cd client`
- `npm install` (the first time)
- `npm start`
